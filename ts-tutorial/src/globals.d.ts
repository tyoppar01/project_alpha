declare global {
      /** date global file export test documentation */
      function formatDate(date: Date): string;

      function auditable(target: object, key: string | symbol);
      
}

export {

}